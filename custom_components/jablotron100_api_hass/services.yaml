reset_problem:
    name: Reset problem
    description: Reset problem.
    target:
        entity:
            integration: jablotron100_api_hass
            domain: binary_sensor
            device_class: problem
